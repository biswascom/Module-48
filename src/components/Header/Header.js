import React from 'react';
import './Header.css';

const Header = () => {
    return (
        <div>
            <nav>
                <a href="#">Home</a>
                <a href="#">About</a>
                <a href="#">Countries Info</a>
                <a href="#">Contact Us</a>
                <a href="#">More</a>
            </nav>
        </div>
    );
};

export default Header;